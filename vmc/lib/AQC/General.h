#ifndef AQC_GENERAL
#define AQC_GENERAL

#include <math.h>
#include <float.h>
#include <complex.h>

#define MIN(X, Y)  ((X) < (Y) ? (X) : (Y))
#define MAX(X, Y)  ((X) > (Y) ? (X) : (Y))
#define SIGN(X) X > 0? 1.0 : -1.0

#endif